{% extends 'base.html.twig' %}

{% block body %}
{% block title %} velo.tn {% endblock %} 
<!-- Page Content -->
  
    
          <div class="text-center" ><h1> Affichage reservation: client </h1> <br/> </div> 

          <br>   <br> 
    <table class="table_striped">
        <tbody>
            <tr>
                <th>Datedeb</th>
                <td>{% if reservation.datedeb %}{{ reservation.datedeb|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Datefin</th>
                <td>{% if reservation.datefin %}{{ reservation.datefin|date('Y-m-d') }}{% endif %}</td>
            </tr>
          
        </tbody>
    </table>
   
        <td>                  
            <a href="{{ path('reservation_edit', { 'id': reservation.id }) }}" class="btn btn-warning "> Modifier </a>
            <a href="{{ path('reservation_delete', { 'id': reservation.id }) }}" class="btn btn-danger "> Supprimer </a>                    
        </td>
         {% endif %}
         <br>       <br>   <br>   <br>   <br>    <br>   <br> 
{% endblock %}
